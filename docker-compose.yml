services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: extblock
      POSTGRES_USER: extblock
      POSTGRES_PASSWORD: extblock
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
